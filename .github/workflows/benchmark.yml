name: Benchmark
on:
  push:
    branches: [main]
  workflow_dispatch: {}
  schedule:
    - cron: '14 0 * * *'

permissions:
  contents: read

jobs:
  build-with-github:
    name: GitHub / ${{ matrix.image }} / ${{ matrix.caching }}
    strategy:
      fail-fast: false
      matrix:
        image:
          - ubuntu-24.04-4
          - ubuntu-24.04-arm
        caching:
          - cache=enabled
          - cache=disabled
    runs-on: ${{ matrix.image }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Determine upstream commit SHA
        id: upstream
        run: echo "sha=$(cd upstream && git rev-parse HEAD)" >> $GITHUB_OUTPUT
      - name: Reclaim some disk space
        run: sudo rm -rf /opt
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v4
        if: ${{ matrix.caching == 'cache=enabled' }}
        with:
          context: https://github.com/PostHog/posthog.git#${{ steps.upstream.outputs.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILDKIT_CONTEXT_KEEP_GIT_DIR=1
      - uses: docker/build-push-action@v4
        if: ${{ matrix.caching == 'cache=disabled' }}
        with:
          context: https://github.com/PostHog/posthog.git#${{ steps.upstream.outputs.sha }}
          build-args: |
            BUILDKIT_CONTEXT_KEEP_GIT_DIR=1

  build-with-runs-on:
    name: RunsOn / ${{ matrix.runner }} / ${{ matrix.caching }}
    strategy:
      fail-fast: false
      matrix:
        runner:
          - image=ubuntu24-full-x64,family=m8azn.xlarge
          - image=ubuntu24-full-arm64,family=r8g.xlarge
        caching:
          - cache=enabled
          - cache=disabled
    runs-on: runs-on=${{ github.run_id }},${{ matrix.runner }},extras=ecr-cache,volume=120gb,env=demo,tag=${{ matrix.caching }}
    steps:
      - uses: runs-on/action@v2
        with:
          show_costs: 'summary'
      - uses: actions/checkout@v5
        with:
          submodules: recursive
      - name: Determine upstream commit SHA
        id: upstream
        run: echo "sha=$(cd upstream && git rev-parse HEAD)" >> $GITHUB_OUTPUT
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v4
        if: ${{ matrix.caching == 'cache=enabled' }}
        env:
          TAG: "${{ env.RUNS_ON_ECR_CACHE }}:posthog-latest"
        with:
          context: https://github.com/PostHog/posthog.git#${{ steps.upstream.outputs.sha }}
          push: true
          tags: ${{ env.TAG }}
          cache-from: type=registry,ref=${{ env.TAG }}
          cache-to: type=registry,ref=${{ env.TAG }},mode=max
          build-args: |
            BUILDKIT_CONTEXT_KEEP_GIT_DIR=1
      - uses: docker/build-push-action@v4
        if: ${{ matrix.caching == 'cache=disabled' }}
        with:
          context: https://github.com/PostHog/posthog.git#${{ steps.upstream.outputs.sha }}
          build-args: |
            BUILDKIT_CONTEXT_KEEP_GIT_DIR=1
      
